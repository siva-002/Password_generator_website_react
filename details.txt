password generation

two components 
-------------
	one for showing the generated pass with clipboard and regenerate option
	below that password strength indicator 

	another for to generate password logic
	 which includes 
		one input type number
		one input type range
		
		three radio button for three oprions
		four checkboxes for four options


******on every change in the option then password re generated****

for generation
-----------

	first checks the user selection
		if only uppercase
			just randomly select uppercase from all uppercase based on length
		if many selection
			just split up user selection based on selected length and generate password for each selection 
			then store those result and swp those 


